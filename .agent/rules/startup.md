# Rule: Session Startup

Whenever a new session starts or when you begin work on a feature, verify if the development servers (Laravel and Vite) are running. If they are not, suggest running the `/startup` workflow or ask the user for permission to start them.
